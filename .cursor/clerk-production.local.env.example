# Clerk PRODUCTION keys — copy to clerk-production.local.env (gitignored)
# Dashboard → switch to Production instance → API Keys
# Or: npx clerk@latest env pull --instance prod

VITE_CLERK_PUBLISHABLE_KEY=pk_live_REPLACE_ME
CLERK_SECRET_KEY=sk_live_REPLACE_ME
VITE_CLERK_ORGANIZATION_ID=org_REPLACE_ME
