---
description: CoreKnot inherits platform agent governance — verify before memory writes
alwaysApply: true
---

# Agent Governance (CoreKnot)

Inherits platform rule: `../../../../.cursor/rules/agent-governance.mdc`

## Boot

1. Read `AGENTS.md` in this package root (memory-first table)
2. Read `memory/obsidian/INDEX.md` + `memory/obsidian/ReportExploration.md` + `memory/obsidian/RecentChanges.md` (canonical; physical path: TSC Platform `memory/obsidian/`)
3. Read `.specify/memory/INDEX.md` only if compatibility context is needed
4. Run `.cursor/skills/coreknot-session-boot/SKILL.md` for non-trivial edits
5. Pick agency agent via agent-menu; record on ledger slice

## Verify bundle (before done / memory patch)

```bash
npm test --prefix client
npm test --prefix server
npm run build --prefix client
```

```bash
node ../../../../.cursor/scripts/agent-memory-gate.mjs verify-and-patch \
  --ledger .cursor/loop-engineering/<slug>-ledger.json \
  --patch '{"satisfaction":"pass","phase":"complete"}' \
  -- "npm test --prefix client && npm test --prefix server"
```

Paths relative to `coreknot/Taskmaster/`.
