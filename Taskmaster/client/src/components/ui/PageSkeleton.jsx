import React from 'react';
import ListPageSkeleton from './ListPageSkeleton';

/** @deprecated name kept for imports — use ListPageSkeleton for list pages. */
const PageSkeleton = (props) => <ListPageSkeleton {...props} />;

export default PageSkeleton;
