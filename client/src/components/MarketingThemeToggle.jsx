import React from 'react';

/** Public pages follow OS color scheme; manual toggle removed to avoid persisting theme before login. */
export default function MarketingThemeToggle() {
  return null;
}
