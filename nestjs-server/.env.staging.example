# Render staging API — preview Supabase (gitignored copy as .env.staging)
# See docs/PREVIEW_SUPABASE_CUTOVER.md Phase C

NODE_ENV=production
PORT=10000
JWT_SECRET=<same-as-production-express>
DATABASE_URL=<from production-hosts.local.json supabase.preview.databaseUrl>
# Unset MONGODB_URI when domain fully on Nest; keep for unmigrated strangler routes
# MONGODB_URI=
