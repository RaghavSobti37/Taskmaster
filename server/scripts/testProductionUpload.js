/**
 * Production upload smoke test (finance + mail template image).
 * Requires credentials that work on the target API.
 *
 * Run:
 *   TEST_API_URL=https://tsccoreknot.com TEST_LOGIN_EMAIL=... TEST_LOGIN_PASSWORD=... node server/scripts/testProductionUpload.js
 */
require('dotenv').config({ path: require('path').join(__dirname, '../.env') });

const BASE = (process.env.TEST_API_URL || 'https://tsccoreknot.com').replace(/\/$/, '');

async function login() {
  const email = (process.env.TEST_LOGIN_EMAIL || process.env.ADMIN_EMAIL || '').trim();
  const password = (process.env.TEST_LOGIN_PASSWORD || process.env.ADMIN_PASSWORD || '').trim();
  if (!email || !password) {
    throw new Error('Set TEST_LOGIN_EMAIL and TEST_LOGIN_PASSWORD (or ADMIN_EMAIL / ADMIN_PASSWORD) in server/.env');
  }

  const res = await fetch(`${BASE}/api/auth/login`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ email, password }),
  });
  const body = await res.json().catch(() => ({}));
  if (!res.ok) {
    throw new Error(`login ${res.status}: ${body.error || body.message || JSON.stringify(body)}`);
  }

  const setCookie = res.headers.getSetCookie?.() || [];
  const cookiePair = setCookie.find((c) => c.startsWith('coreknot_token_v3='));
  if (cookiePair) {
    return cookiePair.split(';')[0];
  }
  if (body.token) {
    return `Bearer ${body.token}`;
  }
  throw new Error('login succeeded but no session cookie or token returned');
}

function authHeader(session) {
  if (session.startsWith('coreknot_token_v3=')) {
    return { Cookie: session };
  }
  if (session.startsWith('Bearer ')) {
    return { Authorization: session };
  }
  return { Authorization: `Bearer ${session}` };
}

async function presign(session, slug, file) {
  const res = await fetch(`${BASE}/api/uploadthing?actionType=upload&slug=${slug}`, {
    method: 'POST',
    headers: {
      ...authHeader(session),
      'content-type': 'application/json',
      'x-uploadthing-package': 'uploadthing/client',
      'x-uploadthing-version': '7.7.4',
    },
    body: JSON.stringify({
      files: [{
        name: file.name,
        size: file.size,
        type: file.type,
        lastModified: file.lastModified || Date.now(),
      }],
      input: null,
    }),
  });
  const text = await res.text();
  if (!res.ok) throw new Error(`presign ${slug} ${res.status}: ${text.slice(0, 500)}`);
  const data = JSON.parse(text);
  return Array.isArray(data) ? data[0] : data;
}

async function uploadToIngest(presigned, file) {
  const form = new FormData();
  form.append('file', file);
  const res = await fetch(presigned.url, {
    method: 'PUT',
    headers: {
      Range: 'bytes=0-',
      'x-uploadthing-version': '7.7.4',
    },
    body: form,
  });
  const text = await res.text();
  if (!res.ok) throw new Error(`ingest ${res.status}: ${text.slice(0, 500)}`);
  const payload = JSON.parse(text);
  const url = payload.ufsUrl || payload.url;
  if (!url) throw new Error(`ingest missing url: ${text.slice(0, 300)}`);
  return url;
}

async function testUpload(session, slug, file, label) {
  process.stdout.write(`[${label}] ${file.name} @ ${BASE} ... `);
  const presigned = await presign(session, slug, file);
  const url = await uploadToIngest(presigned, file);
  console.log('OK');
  console.log(`  url: ${url}`);
}

async function main() {
  console.log(`Production upload test → ${BASE}`);
  const session = await login();
  console.log('Logged in.');

  const healthRes = await fetch(`${BASE}/api/health`);
  const health = await healthRes.json();
  const ut = health?.dependencies?.uploadthing || health?.data?.dependencies?.uploadthing;
  if (ut) {
    console.log(`Health uploadthing: ok=${ut.ok} state=${ut.state} fingerprint=${ut.keyFingerprint || 'n/a'}`);
  } else {
    console.log('Health: uploadthing not reported — deploy latest API for fingerprint checks');
  }

  const jpgBytes = Buffer.from([
    ,
  ]);
  const jpgFile = new File(
    [jpgBytes],
    '19f989_5725323963cd4a0580a3fd807f58cb35~mv2.jpg',
    { type: 'image/jpeg' },
  );

  await testUpload(session, 'financeDocUploader', jpgFile, 'prod/finance-jpg');
  await testUpload(session, 'mailTemplateImageUploader', jpgFile, 'prod/mail-jpg');

  console.log('\nProduction upload OK');
}

main().catch((err) => {
  console.error('\nFAIL', err.message);
  process.exit(1);
});
